{
  "name": "c8y-deploy-poll",
  "version": "0.2.0",
  "description": "Periodically evaluate a Cumulocity deployment and request an operation when a new version is available",
  "source": "src/main.ts",
  "module": "lib/main.js",
  "type": "module",
  "tedge": {
    "mapper": "c8y",
    "files": [
      "poll.sh"
    ]
  },
  "scripts": {
    "build": "esbuild src/main.ts --target=es2018 --bundle --outfile=lib/main.js --format=esm",
    "test": "jest --coverageProvider=v8 --coverage"
  },
  "author": "thin-edge.io",
  "license": "Apache-2.0"
}
