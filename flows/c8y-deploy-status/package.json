{
  "name": "c8y-deploy-status",
  "version": "0.2.0",
  "description": "Report the progress of a deployment (from the c8y-deploy-operation flow) to Cumulocity via the device's digital twin",
  "source": "src/main.ts",
  "module": "lib/main.js",
  "type": "module",
  "tedge": {
    "mapper": "c8y"
  },
  "scripts": {
    "build": "esbuild src/main.ts --target=es2018 --bundle --outfile=lib/main.js --format=esm",
    "test": "jest --coverageProvider=v8 --coverage"
  },
  "author": "thin-edge.io",
  "license": "Apache-2.0"
}
