[input]
mqtt.topics = [
    "te/device/main/service/mosquitto-c8y-bridge/status/health",
    "te/device/main/service/tedge-mapper-bridge-c8y/status/health",
]

[[steps]]
script = "lib/main.js"
interval = "10s"
config.window_size_minutes = "${.params.window_size_minutes}"
config.stats_topic = "${.params.stats_topic}"
